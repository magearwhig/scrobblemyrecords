# Server Configuration
BACKEND_PORT=3001
FRONTEND_PORT=8080
PORT=3001  # Deprecated: Use BACKEND_PORT instead
HOST=127.0.0.1  # Server bind address (127.0.0.1 for localhost only, 0.0.0.0 for all interfaces)
NODE_ENV=development
FRONTEND_URL=http://localhost:8080  # Required for CORS in production

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔐 ENCRYPTION KEY CONFIGURATION (REQUIRED)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# CRITICAL: This application REQUIRES a secure encryption key to protect sensitive
# authentication tokens (OAuth tokens, API keys, session keys) stored locally.
#
# 🚨 SECURITY REQUIREMENTS:
# • Minimum 32 characters long (recommended: 64+ characters)
# • Cryptographically random (high entropy)
# • Never use default, test, or weak patterns
# • Never commit to version control (.env should be in .gitignore)
#
# 🔧 HOW TO GENERATE A SECURE KEY:
# Run this command to generate a secure 256-bit (32-byte) hex key:
#
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Then copy the output and set it below:
#
ENCRYPTION_KEY=
#
# 📝 EXAMPLE (DO NOT USE THESE VALUES):
# ENCRYPTION_KEY=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456
#
# 🔄 KEY ROTATION:
# To rotate keys:
# 1. Backup your data directory
# 2. Clear authentication tokens: DELETE data/settings/user-settings.json
# 3. Generate new key and update ENCRYPTION_KEY
# 4. Re-authenticate with Discogs and Last.fm
#
# ⚠️  WARNING: Changing the encryption key will invalidate all stored tokens!
# Users will need to re-authenticate with both Discogs and Last.fm.
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Discogs API Configuration
DISCOGS_CLIENT_ID=
DISCOGS_CLIENT_SECRET=
DISCOGS_CALLBACK_URL=http://localhost:3001/api/v1/auth/discogs/callback

# Last.fm API Configuration
LASTFM_API_KEY=
LASTFM_SECRET=
LASTFM_CALLBACK_URL=http://localhost:3001/api/v1/auth/lastfm/callback
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# API Authentication (only needed when exposing the server beyond localhost)
#
# This app has no user accounts, so access is controlled by who can reach the
# port. The server REFUSES TO START on a non-loopback HOST without a token.
#
#   API_TOKEN=$(openssl rand -hex 32)
#
# Clients send it as: Authorization: Bearer <token>
# The token travels in plaintext over HTTP -- use TLS or an authenticated
# reverse proxy for anything beyond a trusted LAN. See SECURITY.md.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# API_TOKEN=
# Set when a reverse proxy exposes a loopback-bound server:
# REQUIRE_AUTH=true
